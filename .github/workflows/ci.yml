name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/juanie

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run lint
      - run: bun run build

  deploy:
    needs: quality
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: 登录 GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建并推送镜像
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: 复制迁移文件到服务器
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "migrations/*.sql"
          target: "/tmp/juanie-migrations"
          strip_components: 0

      - name: 触发服务器部署
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # 运行数据库迁移
            echo "🔄 运行数据库迁移..."

            # 确保迁移记录表存在
            kubectl exec -n juanie postgres-0 -- psql -U postgres -d juanie -c "
              CREATE TABLE IF NOT EXISTS \"_migrations\" (
                \"id\" serial PRIMARY KEY,
                \"name\" varchar(255) NOT NULL UNIQUE,
                \"executed_at\" timestamp NOT NULL DEFAULT NOW()
              );
            "

            # 迁移文件列表（按顺序执行）
            MIGRATIONS="000_migration_tracker.sql 001_init.sql"

            for migration in $MIGRATIONS; do
              # 检查是否已执行
              EXISTS=$(kubectl exec -n juanie postgres-0 -- psql -U postgres -d juanie -tAc \
                "SELECT COUNT(*) FROM \"_migrations\" WHERE \"name\" = '$migration'")

              if [ "$EXISTS" = "0" ]; then
                echo "📌 执行迁移: $migration"
                kubectl cp /tmp/juanie-migrations/migrations/$migration juanie/postgres-0:/tmp/$migration
                kubectl exec -n juanie postgres-0 -- psql -U postgres -d juanie -v ON_ERROR_STOP=1 -f /tmp/$migration

                # 记录迁移
                kubectl exec -n juanie postgres-0 -- psql -U postgres -d juanie -c \
                  "INSERT INTO \"_migrations\" (\"name\") VALUES ('$migration')"
                echo "✅ 迁移完成: $migration"
              else
                echo "⏭️  跳过已执行的迁移: $migration"
              fi
            done

            echo "✅ 数据库迁移完成!"

            # 清理临时文件
            rm -rf /tmp/juanie-migrations

            # 触发重新拉取镜像并滚动更新
            kubectl rollout restart deployment/juanie-web -n juanie
            kubectl rollout restart deployment/juanie-worker -n juanie
            kubectl rollout status deployment/juanie-web -n juanie --timeout=5m
            kubectl rollout status deployment/juanie-worker -n juanie --timeout=5m
            echo "✅ 部署完成!"
