apiVersion: apps/v1
kind: Deployment
metadata:
  name: <%= projectId %>
spec:
  replicas: 1  # 始终运行
  template:
    spec:
      containers:
      - name: app
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
