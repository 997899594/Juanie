# Archived Deployment Architecture

This file previously described the retired `values-gitops.yaml` and `juanie-platform` Argo CD
deployment path. It is intentionally no longer current.

Use [`docs/current-architecture.md`](./docs/current-architecture.md) for the active CI + signed
digest + Helm deployment architecture.
