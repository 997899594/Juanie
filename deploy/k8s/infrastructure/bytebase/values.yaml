# Bytebase is optional. Juanie bootstrap creates or references a dedicated
# metadata PostgreSQL database and passes the connection through a Kubernetes
# Secret, so this file only carries stable, non-secret defaults.
