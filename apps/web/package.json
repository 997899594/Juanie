{
  "name": "@juanie/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vue-tsc --noEmit && vite dev",
    "dev:host": "vite dev --host",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit --pretty"
  },
  "dependencies": {
    "@juanie/api": "workspace:*",
    "@juanie/ui": "workspace:*",
    "vue": "^3.5.22",
    "vue-router": "^4.4.5",
    "lucide-vue-next": "^0.460.0"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0"
  }
}
