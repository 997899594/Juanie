{
  "name": "@juanie/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "dev:host": "vite dev --host",
    "dev:check": "vue-tsc --noEmit && vite dev",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit --pretty"
  },
  "dependencies": {
    "@juanie/ui": "workspace:*",
    "vue": "^3.5.22",
    "vue-router": "^4.4.5",
    "lucide-vue-next": "^0.460.0"
  }
}
