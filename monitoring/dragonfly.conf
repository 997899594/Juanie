# Dragonfly 配置文件
# 用于开发环境

# 禁用 ACL（开发环境）
# Dragonfly 默认不启用 ACL，但如果启用了需要配置

# 最大内存
maxmemory 2gb
maxmemory-policy allkeys-lru

# 持久化
save 900 1
save 300 10
save 60 10000

# 日志
loglevel notice

# 网络
bind 0.0.0.0
protected-mode no
